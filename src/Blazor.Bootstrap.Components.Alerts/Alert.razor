@inherits BootstrapComponentBase

<AlertWrapper Color="@Color">
    @if (Heading != null)
    {
        <AlertHeading Size="@HeadingSize">@Heading</AlertHeading>
    }
    @ChildContent
</AlertWrapper>